name: G-SENTINEL AGENT RUNNER - APEX SHORT

on:
  # 1. Frecuencia de ejecución automática 24x5 (Cada 15 min, Lunes a Viernes continuo)
  schedule:
    - cron: '*/15 * * * 1-5'

  # 2. Permitir ejecución manual desde la interfaz de GitHub
  workflow_dispatch:

  # 3. Permitir activación remota vía API / Webhook (PING DE RESPALDO UPTIME GUARD)
  repository_dispatch:
    types: [trigger_sentinel]

permissions:
  contents: write

jobs:
  execute-sentinel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Código
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar Dependencias
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; else pip install yfinance pandas numpy; fi

      - name: Ejecutar Bot G-SENTINEL APEX SHORT
        env:
          # Inyección de claves API
          EXCHANGE_API_KEY: ${{ secrets.EXCHANGE_API_KEY }}
          EXCHANGE_SECRET_KEY: ${{ secrets.EXCHANGE_SECRET_KEY }}
        run: |
          if [ -f main.py ]; then python main.py; else python fetch_apex_short.py; fi

      - name: Sincronización y Commit de Estado JSON (Skip CI)
        run: |
          git config --global user.name "G-SENTINEL-BOT"
          git config --global user.email "bot@g-sentinel.local"
          git add -A
          git diff --quiet && git diff --staged --quiet || (git commit -m "update: Pata 2 APEX SHORT auto-sync [skip ci]" && git push)
